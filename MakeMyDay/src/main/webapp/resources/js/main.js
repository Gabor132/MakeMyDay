
/* global getEvents, getSites, getUsers, getPlans, bubble_namespace */

/**
 * This is the main method that generates the general HTML elements
 * @returns nothing
 */

window.onload = function(){
};

String.prototype.replaceAll = function(search, replacement) {
    var target = this;
    return target.split(search).join(replacement);
};